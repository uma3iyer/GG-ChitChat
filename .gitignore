# Virtual environment
.venv/

# Secrets
.env

# Data (copyrighted transcripts + derived artifacts — never commit).
# Ignore contents but keep the folders via their .gitkeep.
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Local project notes (not for the public repo)
CLAUDE.md
Roadmap.md
DEPLOY.md

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# macOS
.DS_Store
