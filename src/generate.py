"""Assemble the persona prompt + retrieved exemplars and call the LLM.

Skeleton only — no real logic yet.
"""

from __future__ import annotations


def generate_reply(character: str, message: str) -> str:
    """Generate an in-character reply to ``message``.

    Args:
        character: The character to speak as.
        message: The user's message.

    Returns:
        New dialogue in the character's style (never a verbatim transcript line).
    """
    raise NotImplementedError
