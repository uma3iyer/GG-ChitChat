"""Style-fidelity eval: can a judge tell the characters apart?

Skeleton only — no real logic yet.
"""

from __future__ import annotations


def evaluate_style_fidelity() -> dict:
    """Run the style-fidelity eval and return summary metrics.

    Returns:
        A dict of metric names to values (e.g. judge accuracy).
    """
    raise NotImplementedError
